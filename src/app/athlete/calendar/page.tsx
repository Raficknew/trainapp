import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Calendar",
  description: "View your training schedule and calendar",
};

export default function UserCalendarPage() {
  return <div>User Calendar Page</div>;
}
