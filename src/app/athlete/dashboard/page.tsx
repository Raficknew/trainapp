import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
  description: "Your athlete dashboard",
};

export default function UserDashboardPage() {
  return <div>Athlete Dashboard</div>;
}
